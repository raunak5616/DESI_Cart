{
  
  "scripts": {
    "seller-backend": "npm run dev --prefix seller-backend",
    "seller-app": "npm run dev --prefix seller-app",
    "dev": "concurrently \"npm run seller-backend\" \"npm run seller-app\""
},

  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
